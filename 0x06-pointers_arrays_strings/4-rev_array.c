#include <stdio.h>
#include "main.h"

/**
 *reverse_array - See code
 *
 *@a: array
 *@n: Size of a
 *
 *
 */

void reverse_array(int *a, int n)
{
	for (i = 0; n >= 0; i++)
	{
	int[i] = int[n];
	n--;
	}
}
