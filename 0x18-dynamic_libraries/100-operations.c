/**
 * mod - see code
 * @a: number
 * @b: number
 * Return: see code
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * mod - see code
 * @a: number
 * @b: number
 * Return: see code
 */
int sub(int a, int b)
{
	return (a - b);
}

/**
 * mod - see code
 * @a: number
 * @b: number
 * Return: see code
 */
int mul(int a, int b)
{
	return (a * b);
}

/**
 * mod - see code
 * @a: number
 * @b: number
 * Return: see code
 */
int div(int a, int b)
{
	return (a / b);
}

/**
 * mod - see code
 * @a: number
 * @b: number
 * Return: see code
 */
int mod(int a, int b)
{
	return (a % b);
}
